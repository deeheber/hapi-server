TODO:

- connection to a database (probably MongoDB)
- ADD ROUTES
  GET /
  GET /:id
  POST /
  PUT /:id
  DELETE /:id
- ADD OPTIONS for routes
  params 
  optional params
  query string
- Serve static files
- Testing